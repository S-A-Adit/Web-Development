<h2>Join the Club</h2>
<p>Enter the secret member passcode to gain member privileges.</p>

<% if (error) { %><div class="errors"><p><%= error %></p></div><% } %>

<form method="POST" action="/join" class="form">
  <label>Passcode
    <input name="passcode" type="password" required />
  </label>
  <button type="submit">Join</button>
</form>

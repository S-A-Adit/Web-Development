<main class="container">
    <div class="detail-card">
        <h1 class="main-title">Delete Category: <%= category.name %></h1>
        
        <div class="alert alert-danger">
            <p>Are you sure you want to delete this category?</p>
            <p><strong>Warning:</strong> This will also delete all items in this category.</p>
        </div>

        <form action="/categories/<%= category.id %>/delete" method="POST" class="form-group">
            <label for="admin_password" class="form-label">Enter Admin Password to confirm:</label>
            <input type="password" id="admin_password" name="admin_password" class="form-control" required>
            
            <div class="action-links" style="margin-top: 1rem;">
                <button type="submit" class="btn btn-danger">Delete</button>
                <a href="/categories/<%= category.id %>" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</main>